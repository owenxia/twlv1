<%= simple_form_for @experience do |f| %>
	<%= f.input :name %>
	<%= f.input :country %>
	<%= f.input :city %>
	<%= f.text_field :tag_list, value: @experience.tag_list.to_s %>
	<%= f.button :submit %>
<% end %>
